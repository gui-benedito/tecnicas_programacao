let boasVindas = (nome) => {
    return `Bem-vindo aos módulos, ${nome}`
}

let opniao = (oQueVoceAcha) => {
    return `Agora eu acho que usar módulos é ${oQueVoceAcha}`
}

export default boasVindas
export {opniao}